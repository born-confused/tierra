# Tierra Product Website

A Vercel-ready Next.js website for Tierra, a modular eco-friendly wall tile product.

## Run locally

```bash
npm install
npm run dev
```

Then open `http://localhost:3000`.

## Deploy to Vercel

1. Push this folder to a GitHub repository.
2. Import the repo into Vercel.
3. Vercel will detect Next.js automatically.
4. Click deploy.

## Project structure

- `app/layout.js` - shared page layout and metadata
- `app/page.jsx` - homepage content and section navigation
- `app/globals.css` - full styling for the site

## Notes

- Replace `hello@clocktiles.com` with the real team email.
- Replace placeholder design blocks with product renders or photography when ready.
- You can expand this into multi-page navigation later, but it already works well as a high-conversion product landing page.
